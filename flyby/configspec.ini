[dynamodb]

    [[backend]]
        ReadCapacityUnits = integer(default=1)
        WriteCapacityUnits = integer(default=1)

    [[service]]
        ReadCapacityUnits = integer(default=1)
        WriteCapacityUnits = integer(default=1)

    [[target-group]]
        ReadCapacityUnits = integer(default=1)
        WriteCapacityUnits = integer(default=1)

    [[resolver]]
        ReadCapacityUnits = integer(default=1)
        WriteCapacityUnits = integer(default=1)
